# TODO: Task 05 - 實作對話 Agent
# 這個檔案將在 Task 05 填入完整實作


class ConversationAgent:
    """處理諮商對話與即時回應的 Agent（骨架）。"""

    async def run(self, *args, **kwargs) -> dict:
        """執行對話流程（待實作）。"""
        raise NotImplementedError
